#include <stdio.h>

/**
 * main - program that prints the alphabet in lowercase
 *
 * iniatialing variables
 *
 * Return: always 0 (success)
 */
int main(void)
{
	int x;

	for (x = 97; x <= 122; x++;)

	putchar ("x:\n");

	return (0);
}
