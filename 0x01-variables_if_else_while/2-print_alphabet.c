#include <stdio.h>

/**
 * main - program that prints the alphabet in lowercase
 *
 * iniatialing variables
 *
 * Return: always 0 (success)
 */
int main(void)
{
	char ch[97--122];
	int = x;

	for (x = 97; x <= 122; x++)
	{
		putchar (x);
	}
	putchar("\n");

	return (0);
}
